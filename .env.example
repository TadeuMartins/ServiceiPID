# ============================================
# INSTRUÇÕES: Como usar este arquivo
# ============================================
# 1. COPIE este arquivo para criar seu .env:
#    - Linux/Mac: cp .env.example .env
#    - Windows CMD: copy .env.example .env  
#    - Windows PowerShell: Copy-Item .env.example .env
#
# 2. EDITE o arquivo .env (não este .env.example!)
#    e substitua os valores de exemplo pelos reais
#
# 3. O arquivo .env NÃO deve ser commitado no git
#    (já está no .gitignore)
# ============================================

# Groq API Configuration (para chamadas LLM principais)
GROQ_API_KEY=sua-chave-groq-aqui
GROQ_BASE_URL=https://api.groq.com/openai/v1

# OpenAI API Configuration (para embeddings no system_matcher)
# OPENAI_API_KEY=sua-chave-openai-aqui

# Optional: Model Configuration
# PRIMARY_MODEL=gpt-oss-120b
# FALLBACK_MODEL=llama-3.3-70b-versatile

# Optional: Request Timeout (in seconds)
# OPENAI_REQUEST_TIMEOUT=600

# Optional: Server Port
# PORT=8000

# Optional: Reference Excel Path
# REF_XLSX_PATH=referencia_systems.xlsx
